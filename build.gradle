buildscript {
    repositories {
        mavenCentral()
    }
}

apply plugin: 'groovy'
apply plugin: 'project-report'
apply from: 'build-scripts/gradle-mvn-push.gradle'

repositories {
    mavenCentral()
    maven {
        url snapshotRepositoryUrl()
    }
}

sourceCompatibility = JavaVersion.VERSION_1_7
targetCompatibility = JavaVersion.VERSION_1_7

version = VERSION_NAME

task wrapper(type: Wrapper) {
    gradleVersion = '2.4'
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.shazam.fork:fork:1.0.0-RC1'
    compile 'com.android.tools.build:gradle:1.2.2'
}

jar {
    manifest {
        attributes 'Implementation-Title': 'Fork Gradle plugin',
                'Implementation-Version': version,
                'Built-By': System.getProperty('user.name'),
                'Built-Date': new Date(),
                'Built-JDK': System.getProperty('java.version'),
                'Built-Gradle': gradle.gradleVersion
    }
}


